services:
  icon-web:
    build:
      context: .
      dockerfile: Dockerfile
    image: icon-web:latest
    container_name: icon-web
    ports:
      - "${PORT:?PORT is required}:80"
    restart: unless-stopped
